import ConfirmBooking from '@/components/ConfirmBooking';
import React from 'react';

const page = () => {
    return (
        <div>
            <ConfirmBooking />
        </div>
    );
};

export default page;